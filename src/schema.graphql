type Movie {
  id: Int!
  name: String!
  score: Int!
  genre: [String]!
}

type Query {
  movies: [Movie]!
  movie(id: Int!): Movie
}

type Mutation {
  addMovie(name: String!, score: Int!, genre: [String]!): Movie!
  deleteMovie(id: Int!): Boolean!
}